{
  "version": 2,
  "builds": [
    {
      "src": "server/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/public/**", "shared/**"]
      }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "server/index.ts" },
    { "source": "/(.*)", "destination": "server/index.ts" }
  ]
}